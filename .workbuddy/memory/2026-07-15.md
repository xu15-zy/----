# 2026-07-15

## 敦煌纹样网站优化审计
- 审阅了 Next.js+R3F 项目（app/components/data/public），产出 `网站优化建议.md`（P0~P4 分级）。
- 关键发现：
  1. 移动端导航缺失（Nav 链接全部 `hidden md:`，<768px 无法跳转）。
  2. 3D 模型仅 qianfo/yun 接入，rendong/lianhua/shuibo/santu 缺失且根目录 glb 文件名不匹配、未在 public/models/。
  3. 差异动画全为 SVG 占位（public/animations 空）。
  4. 开发占位文案（占位开发版/待替换 MP4）外泄。
  5. 无障碍：reduced-motion 未覆盖循环动画、缺 focus-visible、LayerRecombine 无 aria-pressed。
  6. 性能：字体 @import 阻塞、three.js 未懒加载、Environment preset 外部 CDN 依赖。
- 建议用户先修 P0 三项（移动端导航、补模型、清占位文案）再对外展示。

## 桌面端美观度提升方案（用户明确：不做移动端）
- 产出 `美观提升方案.md`，纯视觉美化，不动基础配色/字体系统。
- 核心判断：站点太「平」，缺纹理、装饰边框、光影层次、真实壁画图。
- 三件低成本高收益：① 宣纸噪点纹理 + 阴影系统；② 联珠纹/卷草角饰装饰边框；③ 接入 public/images 里 17 张从未使用的敦煌实景图（横滚带 + sepia 做旧）。
- 中等投入：区块交替底色、Hero 视觉化（左文右图 / 背景水印 + 竖排侧标）、语义化字号 token。
- 打磨：统一 cubic-bezier(0.16,1,0.3,1) 缓动、卡片 hover 金边+中阴影、链接金色扫光下划线、3D 容器/CTA 暗卡加装饰。

## 方向修正：图形化装饰，不写实
- 用户明确：不要写实照片，只要好看+有文化特点。已重写 `美观提升方案.md` 走「图形化文化装饰」路线。
- 签名级三件套：① Caisson 藻井背景组件（叠套方井+中心圆+联珠，SVG）；② Seal 印章式标签（朱砂方印竖排）；③ 联珠纹+卷草角饰边框（.divider-pearl / .frame-dh）。
- 去掉原「接入真实壁画图」方案；保留宣纸纹理+阴影去平。Hero 改左右布局（左文+右放大线描+藻井衬底）。

## 纹样专题入口交互三改（standalone/index.html → deploy/index.html）
用户第三项指令：删除中央浮层悬停旋转、改卡内旋转、替换模型、网格自动轮播。

### 1. 删除中央浮层，改卡内悬停旋转（Task 39）
- 删除 `#pc-focus` 整块（HTML+CSS+JS showFocus/hideFocus）。
- 每张 `.pc` 卡片内新增 `.ci-render-wrap > img.ci-render`，src 指向 `patterns/${slug}-render.png`（id4 用 `patterns/new-shuibo.png`）。
- CSS：`.pc:hover .ci-render-wrap{opacity:1}` + `.pc:hover .ci-render{animation:pcf-spin 14s linear infinite}`；has-thumb 时渲染层覆盖顶部 160px 缩略图区，非 has-thumb 覆盖中心 72px 图标区，`位置不变`。
- 点击卡片改 `G('pattern',p.id)` 跳转详情，不再弹浮层。

### 2. 替换全部 3D 模型 + 加 DRACO（Task 38）
- 源：`D:\桌面\纹样模型\` 6 个 glb（三兔_draco/云纹/千佛/忍冬/波纹/莲花）。
- 复制到 `standalone/models/` 与 `deploy/models/`，沿用 SLUG 命名（rendong/lianhua/yun/shuibo/qianfo/santu），覆盖旧文件。
- 关键：桌面 `三兔_draco.glb` 是 DRACO 压缩（14.9MB，旧版 uncompressed 366MB），其余 5 个非 draco。
- 为 ModelViewer 加 `DRACOLoader`（`three/addons/loaders/DRACOLoader.js`，decoder 走 `https://cdn.jsdelivr.net/npm/three@0.163.0/examples/jsm/libs/draco/`），新建共享 `_gltf` loader 替换两处 `new GLTFLoader()`。

### 3. 纹样网格自动轮播（Task 41）
- `buildCards()` 内 `startCardAuto()` 每 2.8s 循环给卡片加 `.auto`（放大+金边发光+静态显示渲染图）。
- 悬停 `#cards` 暂停（mouseenter/leave），`.ps` 区块用 IntersectionObserver 进入视口才轮播、离开停止。

### Playwright 验证要点（重要）
- 沙箱可联网（CDN three.js / draco 能加载），但沙箱服务用户浏览器访问不到 → 验证用本地 `python -m http.server` 在 deploy/ 起服务，Playwright 走 http://127.0.0.1。
- **必须等入口动画结束再 `G()`**：`G()` 在 `ok` 为 false 时直接 return。验证要在 `wait_for_function(()=>document.getElementById('nv').classList.contains('vis'))` 后再 `G('pattern',id)`，否则模型看似不加载是误判。
- 模型加载成功判据：`.dz` 有 `hide` 类（`dzHidden:True`）且 `#vh` display block；`#vld.on` 先 true 后 false。
- 验证结论：CARDS=6，悬停 wrapOpacity1+pcf-spin running，AUTO_ACTIVE=1，santu(draco)/lianhua 均加载，PAGE_ERRORS=0。images/thumb-*.jpg|png 404 是 findImg 探扩展名，无害。

## 动效交互优化建议（17:32）
- 输出 `动效交互优化建议.md`：基于现有动效代码（globals.css/Reveal/PatternAnimation/LayerRecombine/ModelViewer）给 P0~P2 优化。
- P0：reduced-motion 全局兜底（现只关 .reveal，未关 dh-* 无限循环）、JS-off 兜底（.reveal 默认 opacity:0 风险）、3D Suspense=null 闪白+Environment CDN 依赖改本地灯光。
- P1：统一缓动 token(--ease-dh cubic-bezier(.16,1,.3,1))、滚动揭示多形态+stagger、3D 进场/拖拽 cursor/视角限制、图层重组动感+hover 联动、差异动画正式化去 MP4 占位（贴合图形化方向）。
- P2：按钮扫光/卡片金边/链接下划线/Nav 指示/路由 template.tsx 过渡/Hero 编排。

## AI 生图素材（图形化装饰方向）
- 用户要求生成美观的敦煌装饰素材图片（不写实）。
- 盘点现有素材：根目录 6 个 SVG 线描（可用作矢量 UI 元素）、public/images/ 17 张实景照（与方向不符弃用）、public/models/ 仅 2 个 glb。
- 用 ImageGen 生成 5 张扁平矢量插画风敦煌图存入 `public/images/ai-generated/`：
  - caisson-hero.png（藻井 Hero 1536²）
  - three-hares.png（三兔共耳徽章 1024²）
  - lotus-medallion.png（莲花团花 1024²）
  - cloud-field.png（祥云柔背景 1536×1024）
  - pearl-frieze.png（联珠分割带 1536×512）
- 色彩统一品牌五色（朱砂#B23A2E/石绿#5A8F6B/石青#2F5D7C/金#C9A227），深青底(#1B2A33) 或米白底(#F4EAD4)。
- 写入 `AI素材清单.md` 含每张的用途说明和 TSX 引用代码示例。

## 去除 AI 水印
- 5 张图右下角 Hunyuan「图片由AI生成」水印用 OpenCV inpaint（INPAINT_TELEA）修复移除。
- 坑：OpenCV 在 Windows 下无法读含中文路径文件，改用 imdecode/imencode 内存读写。
- 自动定位方案误把 frieze 珍珠点当水印 → 改回固定右下角矩形（宽 30%×高 13%）覆盖全部 5 张。
- 程序化残差检测确认框外近白/偏暗文字像素≈0，水印已干净去除。
- 带水印原图与脚本移至 `D:/桌面/workbuddy/web/_watermark_backup/`，`public/images/ai-generated/` 仅留干净图。

## Round 2 修复（三兔卡顿/轮播/去背景，已验证）
- 三兔(santu,id6)卡顿：renderer antialias:false+powerPreference high-performance、toneMappingExposure 1.2→1.0；`_go()` 加 `_hidden/_visible` 守卫（IntersectionObserver+visibilitychange 离屏/切后台暂停渲染）；`_clr()` 释放 GPU（geometry/material dispose）；`setPerf(id)` 三兔像素比降到 1.0（其余 1.5）；全屏渲染同样优化。
- React 轮播(gallery/src/components/PatternGallery.tsx)：加 4s `setInterval` 自动轮播，悬停/切后台暂停；`npm run build` → deploy/carousel.html 等已同步。
- rendong/lianhua 模型去背景：pygltflib 删节点 rendong=`柱体.001`、lianhua=`立方体`+`立方体.001`（standalone/models/ 与 deploy/models/ 均改）。
- 验证：CARDS=6、hover-spin running、gallery 标题 忍冬纹→莲花纹、三模型 meshCount 25/40/32 且 hasBgMesh:False、PAGE_ERRORS=0。

## 交互动效与美观层次优化（18:00）
- 用户指令：先用 git 存版本（可回退）再做动效/美观优化。
- git：项目根 `D:/桌面/workbuddy/web` 非仓库 → `git init`，写 `.gitignore`（排除 node_modules/deploy.zip/根级原始.gb/moxing/shots/_watermark_backup/.next/）。
- 快照基线 `f7b39c8` + 标签 `baseline-pre-enhance`（优化前，可 `git checkout` 回退）。
- 增强改动（standalone/index.html → deploy/index.html，已验证）：
  1. 顶部滚动进度条 `#spbar`（scroll 时 scaleX）。
  2. 右下角回到顶部 `#btt`（滚过 0.6 视口高出现，点击平滑回顶）。
  3. Hero 新增「进入纹样世界」CTA 按钮，点击平滑滚动到 `.ps` 纹样专题。
  4. 导航当前页高亮 `setNav(p)`（首页/千年历史/纹样体系/对应纹样名）。
  5. 入场动效升级为带模糊上浮（`@keyframes lr` 加 `filter:blur(7px)→0` + cubic-bezier(.16,1,.3,1)）。
  6. 修复卡片/符号卡 hover 抬升被 reveal 动画 forwards 锁死：hover 改 `translate` 属性（与 reveal 的 transform 不冲突），过渡同步加 translate。
  7. `prefers-reduced-motion` 降级：关动画/过渡、强制 `.rl/.sb/.stp/.hcd/.slc/.ti` 可见。
- 验证（Playwright, http://127.0.0.1:8765）：PRESENT 三元素齐全、SCROLL spbar scaleX=0.586+btt.show、NAV_ACT=['纹样体系']、CTA 滚动到 .ps=True、CARDS_REVEAL 6/6；仅 25 个 404（findImg 探可选图，无害，非新增）。
- 提交 `9016b8d` + 标签 `v1-enhanced`。两个回退点：baseline-pre-enhance（优化前）、v1-enhanced（优化后）。

## Round 3 修复（图案位置/视频/轮播优化，已验证）
- 用户指令：千佛点阵纹与三兔共耳藻井位置错了；插入 6 个纹样动画视频；修复轮播有时不自动且间隔太长；增加轮播卡片悬停顺时针缓慢旋转。
- 图案位置修正：
  - 用用户提供的参考图替换 `deploy/patterns/qianfo-render.png` 与 `standalone/patterns/qianfo-render.png`（千佛点阵纹）。
  - 用用户提供的参考图替换 `deploy/patterns/santu-render.png` 与 `standalone/patterns/santu-render.png`（三兔共耳藻井），JPG 转 PNG。
  - 同步更新 `gallery/public/patterns/` 中对应两张图，确保 React gallery 重新构建后使用正确素材。
- 视频插入：
  - 从 `D:/桌面/纹样模型/` 复制 6 个 MP4 到 `standalone/videos/` 与 `deploy/videos/`，按 SLUG 重命名（rendong/lianhua/yun/shuibo/qianfo/santu.mp4）。
  - 在 `standalone/index.html` 的 pattern 数据中为每个纹样新增 `video:'videos/{slug}.mp4'`（此前仅 santu 有）。
  - 现有 `buildPattern()` 已会根据 `p.video` 在详情页渲染 `<video controls autoplay muted loop playsinline>`，因此所有纹样详情页现在都有视频。
- React 轮播优化（gallery/src/components/PatternGallery.tsx + gallery/src/index.css）：
  - 自动轮播 bug：把 interval 从依赖 `autoPaused` state 改为依赖 `navigate` + `autoPausedRef` 检查，避免 state 更新导致 interval 被意外拆除；同时加 `lockRef` 检查跳过动画中的 tick。
  - 间隔从 4000ms 缩短到 3000ms。
  - 保留 hover/visibilitychange 暂停。
  - 悬停旋转：新增 `@keyframes pg-spin` + `.pg-card:hover .pg-img { animation: pg-spin 12s linear infinite; }`，并带 `prefers-reduced-motion` 降级。
- 构建同步：`npm run build` → `deploy/carousel.html`、`deploy/carousel-standalone.html`、`deploy/assets/` 已更新。
- 验证（Playwright, http://127.0.0.1:8765）：HOME_CARDS=6；PATTERN_VIDEO_1~6 全部 vb=block 且 src 正确；CAROUSEL_AUTO_ADVANCE 忍冬纹→莲花纹；CAROUSEL_HOVER_SPIN_CSS=True；NO_JS_ERRORS=0。

## 网站部署（CloudStudio 静态部署）
- 部署对象：`deploy/` 纯静态目录（index.html + models 41MB glb + videos 14MB + images/patterns/assets），无需 Node 服务器。
- 方式：workbuddy_cloudstudio_deploy 一键上传到 CloudStudio 沙箱静态服务（端口3000），已验证上线。
- 分享链接：https://794ac2ec3f0a4f809d9b50f2fd6e623f.app.codebuddy.work （沙箱ID 794ac2ec3f0a4f809d9b50f2fd6e623f）。
- 备注：若链接失效，重新运行相同部署命令即可；后续内容更新需重新构建 deploy/ 再部署。

## 千佛/三兔 去除黑色背景（19:09）
- 用户指令：所有千佛点阵纹 / 三兔共耳藻井 图像，去除黑色背景，只保留纹样。
- 范围：deploy/standalone/gallery-public/gallery-dist 下 `qianfo-render.png` 与 `santu-render.png` 共 8 个副本（SVG 的 new-qianfo/new-santu 本就透明无背景，未动）。
- 方法：PIL + numpy。纹样是浅蓝白(L≈188,206,229) 渲染在黑底上，背景占 75%~89%。
  - 边缘连通洪泛（4-邻域从边框暗像素生长）定位「连到边框的背景」，避免误删纹样边缘与孤立暗部。
  - 亮度软过渡 alpha：qianfo soft 55→120、santu soft 25→90；深黑角点 alpha=0(透明)，纹样中心 alpha=255(保留)。
  - RGB 转 RGBA 存回 PNG。
- 结果：8 副本角点 alpha=0、中心 alpha=255；dist 与 public 完全一致（public==dist: True）。
- 验证：served 文件 HTTP 200 / image/png / RGBA / 角点透明；首页与轮播页 img 均 naturalWidth=1072/1351 正常加载；仅 1 条与本次无关的 headless WebGL Shader 警告(3D 查看器，未改 shader)。
- 提交 a4ed193 + 标签 v3-transparent-bg。回退点：baseline-pre-enhance / v1-enhanced / v2-fixes / v3-transparent-bg。

## 千佛/三兔 彻底去除黑色背景（19:09 后追加优化）
- 用户反馈：PNG 去底后网页上仍见黑色背景，要求“一点黑色背景都不能保留”。
- 根因：PNG 本身已透明，但 **网页容器** 与 **图片黑色投影** 导致视觉上仍有黑底。
- 修复内容：
  1. React 轮播 `.pg-card`：`background: transparent`，金色边框/发光，不再显示深色方块。
  2. React 轮播图片：去掉 `img` inline style 里的 `drop-shadow(0 0 3px rgba(0,0,0,.9))` 黑色投影，只保留对应纹样的金色/紫色发光。
  3. 首页 `.pc` 卡片：`background: transparent`，去掉 `.ci-render` 的黑色 drop-shadow，hover 仅金色发光。
  4. 详情页古今对比 `.ci-img.an/.mo`：去掉 `var(--bg2)`/`var(--bg3)` 深色背景，同时 `buildPattern` 中不再通过 JS 设置深色背景兜底。
  5. 图片重新处理：在原有洪泛去底基础上，用膨胀透明区域吃掉边缘暗色光晕（dilate 3px），避免纹样边缘残留黑边。
- 影响文件：
  - `gallery/src/index.css` + `gallery/src/components/PatternGallery.tsx`（已 build 同步到 `deploy/carousel.html`/`carousel-standalone.html`/`deploy/assets/`）。
  - `standalone/index.html`（已同步到 `deploy/index.html`）。
  - `deploy/standalone/gallery-public` 下的 `qianfo-render.png`、`santu-render.png`（gallery-dist 也同步更新）。
- 视觉验证：Playwright 截图显示轮播/首页的千佛、三兔图案均呈“金色/紫色发光、浮于页面背景”效果，无深色方块背景。

## 千佛/三兔 轮播卡片补光（进一步去黑底）
- 问题：v4 后用户反馈轮播页千佛/三兔仍显深色方块，要求“一点黑色背景都不能保留”。
- 根因：`.pg-card` 已透明，但轮播根容器背景 `active.bg` 是深色（千佛 `#33260c`、三兔 `#2c1838`），透明卡片直接透出该深色，视觉上仍像黑底。
- 修复：在 `gallery/src/index.css` 的 `.pg-card` 加径向光晕 + 半透明底色：
  - `background: radial-gradient(circle at 50% 45%, rgba(255,248,230,0.14) 0%, transparent 55%), rgba(255,248,230,0.07);`
  - 纹样中心区域明显发亮，四角色调随主题色（千佛暖金、三兔淡紫）过渡，不再是一块深色方块。
- 构建同步：`npm run build` 后更新 `deploy/carousel.html`、`deploy/carousel-standalone.html`、`deploy/assets/`；并清理旧哈希资源文件。
- 验证：Playwright 截图显示轮播千佛/三兔中心平均亮度 130~150，四角 47~58，图案浮于柔光之上，无明显黑底方块。首页 `.pc` 卡片与详情页古今对比容器未动，仍保持透明。
## 替换全部 6 个纹样图片（波纹/云纹/三兔/忍冬/千佛/莲花）
- 用户指令：用新提供的 6 张图替换敦煌纹样网站中所有原有纹样，以及纹样专题入口悬停后旋转的纹样。
- 源图：
  - 波纹 / 忍冬：用户剪贴板 JPG（白底）。
  - 云纹 / 三兔 / 千佛 / 莲花：`D:\桌面\纹样模型\` 下 PNG（透明背景但带白色 matte，或白底）。
- 处理：PIL+numpy 全局色键去底，根据角点背景色统一去除白色/黑色背景，软 alpha 过渡 + 高斯模糊边缘，保留纹样主体。
- 输出并覆盖到以下位置：
  - `deploy/patterns/`、`standalone/patterns/`、`gallery/public/patterns/`、`gallery/dist/patterns/`。
  - 目标文件名：`rendong-render.png`、`lianhua-render.png`、`yun-render.png`、`new-shuibo.png`、`qianfo-render.png`、`santu-render.png`。
- 同时更新详情页「古今对比」的「新」侧图片：
  - 复制新 PNG 到 `deploy/images/new-{slug}.png` 与 `standalone/images/new-{slug}.png`。
  - 由于 `findImg` 按 jpg→png→webp→jpeg→svg 顺序探测，新增 PNG 会覆盖旧 `new-*.svg`。
- 构建同步：重新 `npm run build`（需 `NODE_OPTIONS=` 避开 `--use-system-ca` 报错），同步 `deploy/carousel.html`/`carousel-standalone.html`/`deploy/assets/`。
- 验证：Playwright 截图确认首页 6 张卡片的悬停旋转纹样、轮播 6 张中心卡片、详情页古今对比「新」侧，均已显示为新纹样，无白底/黑底方块。
- 提交 9a47725 + 标签 `v6-new-patterns`。

## 修复忍冬纹、莲花纹细节损失（仅修改两个纹样）
- 用户反馈：v6 替换后，忍冬纹和莲花纹在网页上细节损失太多，要求只调节这两个纹样，其余保持不变。
- 问题根因：
  - 忍冬：源图被缩放（1345×1095 → 1200×976），且去白底时容差过大，把浅色 3D 纹理也当成背景削掉；alpha 模糊进一步柔化边缘。
  - 莲花：源图本身带 alpha 通道，但之前用统一洪泛法重新去底，柔化/过渡太多，导致发光线条发虚。
- 修复（仅处理两个纹样，其他 4 个未动）：
  - 忍冬：保持原分辨率 1345×1095，改用纯白色色键（tol=18）仅去除纯白背景，保留浅蓝/紫色 3D 纹理；增强对比度 1.15、饱和度 1.2，让图案在页面上更突出。
  - 莲花：直接基于原 PNG 的 alpha 通道做二值化（alpha≥35 保留，其余透明），不做模糊，最大限度保留原线条细节。
- 覆盖文件（仅这两个）：
  - `deploy/patterns/`、`standalone/patterns/`、`gallery/public/patterns/` 下的 `rendong-render.png`、`lianhua-render.png`。
  - 详情页古今对比「新」侧的 `deploy/images/new-rendong.png`、`new-lianhua.png` 与 `standalone/images/` 对应文件。
- 构建同步：`npm run build` 后更新 `deploy/carousel.html`、`carousel-standalone.html`、`deploy/assets/`；并同步 `deploy/index.html`。
- 验证：Playwright 截图显示首页悬停、轮播卡片、详情页的忍冬/莲花纹样线条清晰、3D 纹理可见，无浅色背景残留，符合用户参考图效果。
- 提交 1262df3 + 标签 `v7-rendong-lianhua-detail`。

