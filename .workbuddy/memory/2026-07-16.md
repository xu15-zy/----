# 2026-07-16

## v10：千佛新模型、轮播中文水印、3D 颜色降饱和
- 用用户提供的 `D:/微信/.../千佛(1).glb` 替换 `standalone/models/qianfo.glb` 与 `deploy/models/qianfo.glb`。
- React 轮播（gallery/src/components/PatternGallery.tsx）巨型背景水印由英文 `p.en` 改为中文 `p.name`（如「莲花纹」），重新构建并同步到 `deploy/carousel.html`、`deploy/carousel-standalone.html`、`deploy/assets/`。
- 3D 模型颜色降饱和/明度：`FRESCO_COLORS` 金/石青/赭石/朱砂全部改用更柔和的低饱和色，`emissiveIntensity` 从 0.10 降到 0.06；场景主光/环境光/鼠标跟随光同步减弱。
- Playwright 验证：新千佛模型加载正常且呈柔和赭石色；轮播背景显示中文水印；忍冬石青、莲花朱砂、云/水波石青、三兔多色均明显降饱和，更耐看。
- commit d123662 + tag `v10-qianfo-zh-muted`。
